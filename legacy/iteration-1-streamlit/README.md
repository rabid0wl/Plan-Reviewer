# Legacy Iteration 1 (Streamlit)

This folder contains the first-iteration Streamlit prototype and related packaging/config files.

- `plan_reviewer.py` - original Streamlit UI prototype.
- `.streamlit/config.toml` - Streamlit local server config.
- `pyproject.toml` - packaging entrypoint config used by that iteration.

These files are archived for historical reference only. Active development is now the CLI pipeline under `src/`.
